---
controller_hosts:
  - name: "localhost"
    description: !unsafe 'AAP host'
    inventory: "Demo Inventory"
    variables:
      ansible_connection: local
      ansible_python_interpreter: !unsafe '{{ ansible_playbook_python }}'
...
